/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  return knex.raw(`
      ALTER TABLE subscription_profiles DROP COLUMN IF EXISTS planned_start_date;
        ALTER TABLE subscription_profiles DROP COLUMN IF EXISTS planned_end_date;
  `);
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  
};
